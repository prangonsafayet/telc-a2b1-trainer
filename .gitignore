.DS_Store
Thumbs.db
node_modules/
*.zip
dist/
.vite/
*.log
.env
.env.*
.netlify/
!.env.example

# Playwright
test-results/
playwright-report/
blob-report/
.playwright/

# Operational notes kept locally, deliberately not published on the portfolio repo.
docs/*.local.md
.superpowers/

# Incremental TypeScript build caches — the build writes these to node_modules/.tmp.
*.tsbuildinfo

# Playwright MCP scratch output (page snapshots, console logs).
.playwright-mcp/

# Bridge-sync leftovers the sandboxed cloud session cannot delete; safe to remove.
_to_delete/
*.tgz
